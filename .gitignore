# Environment / secrets — never commit real credentials
.env
.env.local
*.env
!.env.example

# Build artifacts (binaries only; do not ignore the cmd/vili source tree)
/bin/
/dist/
*.exe
/vili
/vili.exe

# Test / coverage output
coverage.out
*.coverprofile
*.out
*.test

# AgentHub coordination state (session metadata, message board)
.agenthub/

# Claude Code session worktrees
.claude/

# Editor / OS noise
.idea/
.vscode/
.DS_Store
